/**
 *
 * Fonts used:
 * 		default = Roboto
 * 		cursive = Shadows Into Light
 *
 * Fonts are stored in /assets/fonts[cursive.ttf, default.tff]
 *
 */

export const fonts = {
	default: "default",
	cursive: "cursive",
};
