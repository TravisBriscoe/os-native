import { colors } from "../colors/colors";
import { fonts } from "../font/font";
import { fontSizes, spacing, screenHeight, screenWidth } from "../sizes/sizing";

const themes = { colors, fonts, fontSizes, spacing, screenHeight, screenWidth };

export default themes;
